/**
 * Defines the state of the game.
 */
public enum GameState {
        /** Game State. */
        MAP_STATE,
        /** Game State. */
        MENU_STATE,
        /** Game State. */
        COMBAT_STATE,
        /** Game State. */
        WIN_STATE
}
